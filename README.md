This project analyzes monthly revenue and order volume using SQL on an online_sales dataset.
Aggregation functions like SUM() and COUNT(DISTINCT) are used to summarize data.
strftime() extracts month and year for grouping in SQLite.
The top 3 revenue-generating months are also identified.







